# ---------------------------------------------------------------------------
# requirements.txt
# Dependencies are added incrementally, phase by phase. Each addition is
# explained in the chat/commit history at the point it was introduced.
# ---------------------------------------------------------------------------

# --- Phase 1: Project foundation ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9      # required by FastAPI for UploadFile / form parsing
python-dotenv==1.0.1
streamlit==1.38.0
requests==2.32.3
pytest==8.3.3
httpx==0.27.2                # used by FastAPI TestClient

# --- Phase 2: File ingestion (PDF / DOCX / PPTX / images + OCR) ---
PyMuPDF==1.24.10              # PDF text extraction, page rendering (fitz)
python-docx==1.1.2            # DOCX reading/writing
python-pptx==1.0.2            # PPTX reading/writing
pytesseract==0.3.13           # OCR wrapper (requires system Tesseract binary)
Pillow==10.4.0                # image handling for OCR

# --- Phase 4: LLM provider abstraction ---
google-genai==0.7.0            # Gemini API client
openai==1.51.0                 # OpenAI-compatible client (also used for Groq via base_url)

# --- Phase 5: Enterprise RAG (embeddings + vector store) ---
# Using fastembed (ONNX runtime) instead of sentence-transformers+torch: same
# family of local embedding models, no API key required, but ~300MB instead
# of several GB (torch + CUDA deps). See README "Embeddings" section for the
# tradeoff and how to switch to full sentence-transformers if you prefer.
fastembed==0.3.6
faiss-cpu==1.9.0                # local vector store (default / fallback)
pinecone==5.3.1                 # cloud vector store (used when PINECONE_API_KEY is set)

# --- Phase 6: Web Research Agent ---
tavily-python==0.5.0             # real web search API (used when TAVILY_API_KEY is set)
